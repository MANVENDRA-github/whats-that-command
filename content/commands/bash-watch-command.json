{
  "id": "bash-watch-command",
  "command": "watch -n <seconds> '<command>'",
  "description": "Re-run a command at a fixed interval and show the latest output",
  "tool": "bash",
  "category": "monitor",
  "tags": [
    "repeat every second",
    "live refresh",
    "keep checking",
    "auto re-run"
  ],
  "intents": [
    "re-run a command every few seconds and show changes",
    "poll a command and see its output update live",
    "keep refreshing kubectl get pods automatically",
    "auto-rerun the same command on a timer",
    "live monitor of a command's output without scripting",
    "watch a counter or status change in the terminal",
    "rerun a check periodically until something changes"
  ],
  "example": "watch -n 1 'kubectl get pods'",
  "danger": false,
  "related": [
    "bash-tail-follow"
  ]
}
