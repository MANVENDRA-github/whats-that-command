{
  "id": "docker-run-restart",
  "command": "docker run --restart <policy> <image>",
  "description": "Set restart policy: no, on-failure, always, or unless-stopped",
  "tool": "docker",
  "category": "containers",
  "tags": [
    "auto restart",
    "restart policy",
    "survive reboot"
  ],
  "intents": [
    "make a container restart automatically if it dies",
    "survive a docker daemon restart",
    "set the restart policy on a container"
  ],
  "example": "docker run --restart unless-stopped nginx",
  "danger": false,
  "related": []
}
