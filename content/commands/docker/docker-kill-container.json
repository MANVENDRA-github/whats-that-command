{
  "id": "docker-kill-container",
  "command": "docker kill <container>",
  "description": "Force-stop a running container immediately with SIGKILL",
  "tool": "docker",
  "category": "containers",
  "tags": [
    "force stop",
    "hard kill",
    "not responding",
    "abort container"
  ],
  "intents": [
    "force stop a container that wont stop normally",
    "container is stuck, kill it immediately",
    "hard kill a docker container",
    "send sigkill to a container right now",
    "abort a runaway container without waiting for it",
    "docker stop is too slow, just kill the container",
    "container is hung, end it forcibly"
  ],
  "example": "docker kill stuck-job",
  "danger": true,
  "related": [
    "docker-stop-container"
  ]
}
