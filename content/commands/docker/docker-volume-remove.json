{
  "id": "docker-volume-remove",
  "command": "docker volume rm <name>",
  "description": "Delete a named volume and all the data stored inside it (fails if a container still uses it)",
  "tool": "docker",
  "category": "volumes",
  "tags": [
    "delete data",
    "drop storage",
    "wipe database",
    "destroy mount",
    "get rid of"
  ],
  "intents": [
    "delete a specific docker volume",
    "wipe a postgres data volume i no longer need",
    "drop a named volume and all the data inside it",
    "destroy a docker volume permanently",
    "get rid of one stored volume by name",
    "remove the data stored in a named volume",
    "delete the database storage volume to start fresh"
  ],
  "example": "docker volume rm postgres-data",
  "danger": true,
  "related": [
    "docker-volume-list",
    "docker-volume-prune"
  ]
}
